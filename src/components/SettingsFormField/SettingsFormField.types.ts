/**
 * SettingsFormField component types
 */

import type { InputProps } from '../Input/Input.types'

export interface SettingsFormFieldProps extends InputProps {
  /**
   * No additional props - this is a wrapper around Input
   * All Input props are supported
   */
}
