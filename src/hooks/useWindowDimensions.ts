/**
 * useWindowDimensions – re-export from react-native for migration parity with @unicornlove/ui
 * Use for dimension-dependent layout. Prefer useResponsive() for breakpoint-based logic.
 */

export { useWindowDimensions } from 'react-native'
